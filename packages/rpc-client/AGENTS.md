# AI Development Rules — RPC Client

This package is a framework-neutral client over `@product-foundation/rpc`.
Keep it independent from React, TanStack Query, product configuration and app
shells. It owns fetch adaptation, cancellation, validation and typed errors.
