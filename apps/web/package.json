{
  "name": "@app/web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:dependencies": "pnpm --dir ../.. build:frontend-dependencies",
    "dev": "pnpm build:dependencies && vite",
    "dev:runtime": "vite",
    "build": "pnpm build:dependencies && vite build",
    "build:runtime": "vite build",
    "preview": "vite preview",
    "check": "pnpm build:dependencies && pnpm check:package",
    "check:package": "tsc --noEmit"
  },
  "dependencies": {
    "@app/frontend-app": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "sass": "^1.89.0",
    "typescript": "^5.8.0",
    "vite": "^6.0.0"
  }
}
